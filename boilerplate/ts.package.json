{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "author": "Dustin Stiles <duwstiles@gmail.com>",
  "scripts": {
    "build": "tsc",
    "build:clean": "rm -rf dist",
    "build:watch": "tsc --watch",
    "lint": "tslint -p tsconfig.json",
    "start": "node index.js",
    "test": "jest --config=jest.config.js",
    "test:cov": "npm test -- --coverage",
    "test:watch": "npm test -- --watch"
  }
}
